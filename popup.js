const btn=document.querySelector('#capture'),status=document.querySelector('#status'),fill=document.querySelector('#fill');
btn.onclick=async()=>{btn.disabled=true;status.textContent='Preparando captura...';fill.style.width='3%';try{const [tab]=await chrome.tabs.query({active:true,currentWindow:true});if(!tab||/^(chrome|edge|about|chrome-extension):/.test(tab.url||''))throw Error('Chrome no permite capturar esta página con extensiones.');await chrome.runtime.sendMessage({type:'START_CAPTURE',tabId:tab.id,windowId:tab.windowId,url:tab.url,title:tab.title});status.textContent='Captura iniciada. Puedes cerrar este panel.';}catch(e){status.textContent=e.message;btn.disabled=false;fill.style.width='0%'}};
chrome.runtime.onMessage.addListener(m=>{if(m.type==='PROGRESS'){const p=Math.round(m.current/m.total*100);fill.style.width=p+'%';status.textContent=`Capturando ${m.current} de ${m.total} · ${p}%`}if(m.type==='DONE'){fill.style.width='100%';status.textContent='Captura completada';btn.disabled=false}if(m.type==='ERROR'){status.textContent=m.message;btn.disabled=false}});
