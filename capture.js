(()=>{if(window.__fullPageCaptureInstalled)return;window.__fullPageCaptureInstalled=true;let original=null,hidden=[];
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
function metrics(){const d=document.documentElement,b=document.body;return {width:innerWidth,height:innerHeight,totalHeight:Math.max(d.scrollHeight,b?.scrollHeight||0,d.offsetHeight,b?.offsetHeight||0),dpr:devicePixelRatio||1,scrollY:scrollY};}
function hideFloating(){hidden=[];document.querySelectorAll('body *').forEach(el=>{const p=getComputedStyle(el).position;if(p==='fixed'||p==='sticky'){hidden.push([el,el.style.getPropertyValue('visibility'),el.style.getPropertyPriority('visibility')]);el.style.setProperty('visibility','hidden','important')}})}
function restoreFloating(){hidden.forEach(([el,v,p])=>v?el.style.setProperty('visibility',v,p):el.style.removeProperty('visibility'));hidden=[]}
chrome.runtime.onMessage.addListener((m,s,send)=>{(async()=>{if(m.type==='PREPARE'){original={x:scrollX,y:scrollY,scrollBehavior:document.documentElement.style.scrollBehavior};document.documentElement.style.scrollBehavior='auto';scrollTo(0,0);await sleep(500);send(metrics())}else if(m.type==='SCROLL'){if(m.hide)hideFloating();else restoreFloating();scrollTo(0,m.y);await sleep(m.wait||650);send({actualY:scrollY,...metrics()})}else if(m.type==='RESTORE'){restoreFloating();if(original){scrollTo(original.x,original.y);document.documentElement.style.scrollBehavior=original.scrollBehavior}send(true)}})();return true});})();
